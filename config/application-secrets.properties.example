# =============================================================================
# 本地開發機敏設定 (此檔案不提交到 Git)
# =============================================================================
# 使用方式:
#   1. 複製此檔案: cp application-secrets.properties.example application-secrets.properties
#   2. 編輯填入實際值
#   3. 啟動應用程式: ./gradlew bootRun
#
# 屬性名稱與 GCP Secret Manager 的 secret 名稱一致
# 本地: ${docmcp-xxx} 從此檔案讀取
# GCP:  ${sm@docmcp-xxx} 從 Secret Manager 讀取
# =============================================================================

# Google GenAI API Key (用於 Embedding)
# 取得方式: https://aistudio.google.com/app/apikey
docmcp-google-api-key=your-google-api-key-here

# GitHub Personal Access Token (用於同步私有倉庫文件)
# 取得方式: https://github.com/settings/tokens
docmcp-github-token=your-github-token-here

# 資料庫連線 (可選，若需覆蓋預設值)
# docmcp-db-url=jdbc:postgresql://localhost:5432/mydatabase
# docmcp-db-username=myuser
# docmcp-db-password=secret
