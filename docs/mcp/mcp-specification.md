# Model Context Protocol (MCP) 規格研究

> 最後更新：2026-01-19
> 難度：中級
> 前置知識：JSON-RPC 2.0、LLM 基本概念

## 目錄

1. [簡介](#簡介)
2. [核心架構](#核心架構)
3. [Server 能力](#server-能力)
4. [Client 能力](#client-能力)
5. [傳輸協議](#傳輸協議)
6. [安全考量](#安全考量)
7. [最佳實踐](#最佳實踐)
8. [參考來源](#參考來源)

---

## 簡介

### 什麼是 MCP？

Model Context Protocol (MCP) 是一個開放標準，用於實現 LLM 應用程式與外部資料來源和工具之間的無縫整合。MCP 由 Anthropic 於 2024 年 11 月發布，並於 2025 年 12 月捐贈給 Linux Foundation 下的 Agentic AI Foundation (AAIF)。

MCP 的設計靈感來自 Language Server Protocol (LSP)，LSP 標準化了開發工具對程式語言的支援方式，而 MCP 則標準化了 AI 應用程式如何整合額外的上下文和工具。

### 為什麼要學？

- **標準化整合**：統一 AI 應用與外部系統的連接方式
- **生態系統支援**：被 OpenAI、Anthropic 等主要 AI 廠商採用
- **降低開發成本**：一次實作，多處使用
- **提升 AI 能力**：讓 LLM 能夠存取即時資料和執行外部操作

### 適合誰？

- AI 應用程式開發者
- 希望為 LLM 提供工具和資料的後端開發者
- 企業 AI 整合架構師

---

## 核心架構

### 通訊模型

```
┌─────────────────────────────┐
│    Host Application         │
│    (LLM 應用程式)            │
├─────────────────────────────┤
│    Client                   │
│    (Host 內的連接器)          │
├─────────────────────────────┤
│    JSON-RPC 2.0 Transport   │
└────────────┬────────────────┘
             │
        [Connection]
             │
┌────────────▼────────────────┐
│    MCP Server               │
│    (提供上下文與能力的服務)    │
├─────────────────────────────┤
│  ┌───────────────────────┐  │
│  │ Resources (資源)       │  │
│  │ Tools (工具)           │  │
│  │ Prompts (提示模板)     │  │
│  └───────────────────────┘  │
└─────────────────────────────┘
```

### 主要參與者

| 角色 | 說明 |
|------|------|
| **Host** | 發起連接的 LLM 應用程式 (如 Claude Desktop、Cursor) |
| **Client** | Host 應用程式內的連接器，負責與 Server 通訊 |
| **Server** | 提供上下文和能力的服務，可以是本地程序或遠端服務 |

### 協議基礎

- **協議**：JSON-RPC 2.0
- **連接類型**：Stateful（有狀態的會話）
- **核心功能**：Server 和 Client 能力協商

### 生命週期

1. **初始化 (Initialization)**
   - Client 和 Server 交換協議版本
   - 協商支援的能力
   - 交換元資料

2. **訊息交換 (Message Exchange)**
   - Request-Response 配對
   - 單向 Notifications
   - 基於協商的能力進行通訊

3. **終止 (Termination)**
   - 透過傳輸層關閉連接
   - 處理不可恢復的錯誤

---

## Server 能力

MCP Server 可以提供三種主要能力：

### 1. Tools (工具)

Tools 是 **模型控制** 的能力，允許 LLM 發現並自動調用外部函數。

#### 能力宣告

```json
{
  "capabilities": {
    "tools": {
      "listChanged": true
    }
  }
}
```

#### 工具定義

| 欄位 | 必填 | 說明 |
|------|------|------|
| `name` | ✅ | 唯一識別符 (1-128 字元) |
| `description` | ✅ | 人類可讀的功能說明 |
| `inputSchema` | ✅ | JSON Schema 參數定義 |
| `title` | ❌ | UI 顯示名稱 |
| `outputSchema` | ❌ | 預期輸出結構 |
| `annotations` | ❌ | 工具行為元資料 |

#### 工具範例

```json
{
  "name": "get_weather",
  "title": "Weather Information Provider",
  "description": "Get current weather information for a location",
  "inputSchema": {
    "type": "object",
    "properties": {
      "location": {
        "type": "string",
        "description": "City name or zip code"
      }
    },
    "required": ["location"]
  }
}
```

#### 調用工具

**Request:**
```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "method": "tools/call",
  "params": {
    "name": "get_weather",
    "arguments": {
      "location": "New York"
    }
  }
}
```

**Response:**
```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "result": {
    "content": [
      {
        "type": "text",
        "text": "Current weather in New York:\nTemperature: 72°F"
      }
    ],
    "isError": false
  }
}
```

### 2. Resources (資源)

Resources 允許 Server 暴露資料，為 LLM 提供上下文，如檔案、資料庫 schema 或應用程式特定資訊。每個資源由 URI 唯一識別。

#### 能力宣告

```json
{
  "capabilities": {
    "resources": {
      "subscribe": true,
      "listChanged": true
    }
  }
}
```

#### 資源定義

```json
{
  "uri": "file:///project/src/main.rs",
  "name": "main.rs",
  "title": "Rust Application Main File",
  "description": "Primary application entry point",
  "mimeType": "text/x-rust"
}
```

#### URI 方案

| 方案 | 用途 |
|------|------|
| `https://` | 可從 Web 直接存取的資源 |
| `file://` | 類檔案系統資源 |
| `git://` | Git 版本控制整合 |
| 自定義 | 必須符合 RFC 3986 |

#### 資源模板 (URI Templates)

支援 RFC 6570 URI 模板，允許參數化資源：

```json
{
  "uriTemplate": "file:///{path}",
  "name": "Project Files",
  "description": "Access files in the project directory"
}
```

### 3. Prompts (提示模板)

Prompts 是 **使用者控制** 的結構化訊息和指令，用於與 LLM 互動。

#### 能力宣告

```json
{
  "capabilities": {
    "prompts": {
      "listChanged": true
    }
  }
}
```

#### Prompt 定義

```json
{
  "name": "code_review",
  "title": "Request Code Review",
  "description": "Asks the LLM to analyze code quality",
  "arguments": [
    {
      "name": "code",
      "description": "The code to review",
      "required": true
    }
  ]
}
```

#### 取得 Prompt

**Request:**
```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "method": "prompts/get",
  "params": {
    "name": "code_review",
    "arguments": {
      "code": "def hello():\n    print('world')"
    }
  }
}
```

**Response:**
```json
{
  "jsonrpc": "2.0",
  "id": 2,
  "result": {
    "description": "Code review prompt",
    "messages": [
      {
        "role": "user",
        "content": {
          "type": "text",
          "text": "Please review this Python code:\ndef hello():\n    print('world')"
        }
      }
    ]
  }
}
```

---

## Client 能力

Client 可以提供雙向能力：

### 1. Sampling (取樣)

允許 Server 請求 LLM 取樣，實現代理行為和遞迴 LLM 互動。

- 需要使用者明確批准
- Server 對 Prompt 的可見性有限

### 2. Roots (根目錄)

Server 發起的 URI/檔案系統邊界查詢。

- 定義操作範圍
- 設定檔案系統或 URI 限制

### 3. Elicitation (引出)

Server 發起的額外使用者資訊請求。

- 互動式資料收集
- 需要使用者同意

---

## 傳輸協議

### STDIO (標準輸入/輸出)

- **用途**：本地子程序 Server
- **特點**：
  - 以換行符分隔的 JSON-RPC
  - stdin/stdout 進行通訊
  - stderr 保留給日誌
  - Client 管理生命週期

### HTTP (含 SSE)

- **用途**：獨立/遠端程序
- **特點**：
  - 單一 `/mcp` 端點
  - POST (client→server)
  - GET (server→client SSE 串流)
  - 遠端使用需 HTTPS
  - 透過 Header 支援 Session 管理

### 傳輸比較

| 傳輸方式 | 使用場景 | 最適合 |
|----------|----------|--------|
| **stdio** | 本地子程序 Server | 單一 Client，最小開銷 |
| **HTTP** | 獨立/遠端程序 | 多 Client，Web 標準協議 |

---

## 安全考量

### 核心原則

#### 1. 使用者同意與控制

- 所有資料存取都需明確的使用者同意
- 使用者保留對共享資料的控制權
- 清晰的 UI 用於審查/授權活動

#### 2. 資料隱私

- Host 必須在向 Server 暴露使用者資料前獲得明確同意
- 未經同意不得傳輸資源資料
- 需要適當的存取控制

#### 3. 工具安全

- Tools = 任意程式碼執行（需謹慎對待）
- 工具描述是**不可信的**（除非來自可信 Server）
- 調用任何工具前需明確的使用者同意
- 使用者必須了解工具行為

### 安全最佳實踐

#### Server 必須 (MUST)：
- 驗證所有工具輸入
- 實作適當的存取控制
- 對工具調用進行速率限制
- 清理工具輸出

#### Client 應該 (SHOULD)：
- 對敏感操作提示使用者確認
- 在調用前顯示工具輸入（防止惡意外洩）
- 在傳遞給 LLM 前驗證結果
- 為工具調用實作超時
- 記錄工具使用以供審計

### 2025 年已知安全問題

2025 年 4 月，安全研究人員發現 MCP 存在多個未解決的安全問題：

- **Prompt 注入**：惡意輸入可能操控 LLM 行為
- **工具權限**：組合工具可能導致檔案外洩
- **冒名工具**：類似名稱的工具可能靜默替換可信工具

---

## 最佳實踐

### ✅ 推薦做法 (Do's)

#### 1. 單一職責

每個 MCP Server 應有一個明確、定義良好的目的。

```
❌ 錯誤：建立包含所有功能的單體 "mega-server"
✅ 正確：建立專注的服務
   ├── Database Server
   ├── File Server
   ├── API Gateway
   └── Email Server
```

#### 2. 領域感知的工具設計

避免純 CRUD 操作，改用領域感知的高階動作：

```
❌ 錯誤：create_record, update_row, delete_entry
✅ 正確：submit_expense_report, approve_leave_request, schedule_meeting
```

#### 3. 防禦深度安全

在多個層次實作保護：
- 網路隔離
- 認證
- 授權
- 輸入驗證
- 輸出清理

#### 4. 失敗安全設計

實作優雅降級：
- Circuit breakers
- 快取
- 速率限制

### ❌ 應避免的做法 (Don'ts)

1. **不要**使用自定義認證方法取代 OAuth 2.1
2. **不要**在 HTTP 傳輸中使用明文（必須使用 HTTPS）
3. **不要**信任工具描述（除非來自可信 Server）
4. **不要**跳過使用者確認就執行敏感操作
5. **不要**在錯誤訊息中暴露敏感資訊

### 💡 實務技巧 (Tips)

| 技巧 | 說明 |
|------|------|
| **本地 Server 綁定 localhost** | 綁定到 127.0.0.1 以防止 DNS 重綁定攻擊 |
| **使用環境變數管理憑證** | 不要在程式碼中硬編碼認證資訊 |
| **實作健康檢查** | 多組件健康驗證 (database, cache, external APIs) |
| **結構化日誌** | 使用結構化日誌和指標收集以便監控 |

### ⚠️ 常見錯誤與陷阱

| 錯誤 | 後果 | 解決方案 |
|------|------|----------|
| 初始化失敗 | 連接無法建立 | 驗證協議版本協商 |
| 訊息解析錯誤 | 通訊中斷 | 驗證 JSON-RPC 格式合規性 |
| 超時錯誤 | 操作失敗 | 增加限制或實作進度通知 |
| 傳輸錯誤 | 連接不穩定 | 檢查程序生命週期 (stdio) 或 HTTPS 配置 (HTTP) |

---

## 參考來源

以下是本文件的參考資料：

| 優先級 | 來源類型 | 名稱 | 說明 | 連結 |
|--------|----------|------|------|------|
| 🥇 | 官方文件 | MCP Specification | MCP 官方規格 (2025-11-25) | [modelcontextprotocol.io](https://modelcontextprotocol.io/specification/2025-11-25) |
| 🥇 | 官方 GitHub | modelcontextprotocol | 規格與文件儲存庫 | [GitHub](https://github.com/modelcontextprotocol/modelcontextprotocol) |
| 🥇 | 官方文件 | MCP Tools Specification | 工具規格詳細說明 | [Tools Spec](https://modelcontextprotocol.io/specification/2025-11-25/server/tools) |
| 🥇 | 官方文件 | MCP Resources Specification | 資源規格詳細說明 | [Resources Spec](https://modelcontextprotocol.io/specification/2025-11-25/server/resources) |
| 🥇 | 官方文件 | MCP Prompts Specification | 提示模板規格詳細說明 | [Prompts Spec](https://modelcontextprotocol.io/specification/2025-11-25/server/prompts) |
| 🥈 | 企業部落格 | Anthropic News | MCP 發布公告 | [Anthropic](https://www.anthropic.com/news/model-context-protocol) |
| 🥉 | 英文文章 | MCP Best Practices | 最佳實踐指南 | [modelcontextprotocol.info](https://modelcontextprotocol.info/docs/best-practices/) |
| 🥉 | 英文文章 | MCP Server Development Guide | Server 開發指南 | [GitHub](https://github.com/cyanheads/model-context-protocol-resources/blob/main/guides/mcp-server-development-guide.md) |
| 4️⃣ | 社群 | Wikipedia | MCP 維基百科條目 | [Wikipedia](https://en.wikipedia.org/wiki/Model_Context_Protocol) |

---

## 決策記錄

| 日期 | 決策項目 | 選擇 | 原因 |
|------|----------|------|------|
| 2026-01-19 | 規格版本 | 2025-11-25 | 最新穩定版本 |
| 2026-01-19 | 治理單位 | Linux Foundation AAIF | 2025年12月已由 Anthropic 捐贈 |
